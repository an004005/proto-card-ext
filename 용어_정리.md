# 게임 용어 정리 (검토용)

> 목적: 코드/데이터/UI 전반에서 쓰이는 게임 용어를 한 곳에 모아 **통일된 한글 명칭**을 정하기 위한 문서입니다.
> `카드_전투_스펙.md`의 §0, §4.2, §4.3에 있던 기존 용어표 + 코드 전수조사로 찾은 용어를 합쳤습니다.
> **이 문서를 수정해서 확정 명칭/추가 용어를 표시해주시면, 그 내용대로 코드(데이터 텍스트, UI 라벨, 필요시 내부 키)를 수정하겠습니다.**

표기 규칙:
- `내부 키`: 코드상의 영문 식별자 (kind/status/필드명 등). 원칙적으로 안 바꿔도 되지만, 바꾸고 싶으면 표시해주세요.
- **현재 상태**: 지금 코드/문서에 실제로 쓰이는 한글 표현(들). 여러 개면 불일치 표시.
- **확정 명칭**: 비워둠 — 여기에 원하는 용어를 적어주세요. 지금 표현 그대로면 "유지"라고만 적어도 됩니다.

---

## 카테고리 개요

| 대분류 | 하위 섹션 | 포함 범위 |
|---|---|---|
| 🔴 불일치 | §1 | 지금 코드 안에서 표현이 갈리는 용어만 모음 (최우선 검토) |
| 자원 | §2 | 에너지/탄약/과부화/단계 — 소모·축적되는 수치형 자원 |
| 전투 방어 자원 | §3 | 갑옷/방어도/기습/반사 — 피해를 막거나 되돌리는 전투 메커닉 |
| 상태이상 (버프/디버프) | §4 | `applyStatus`로 부여되는 턴제 스택형 효과 |
| 모듈 파워 | §5 | `player.powers`에 등록되는 지속형 패시브 (상태이상과 별개 시스템) |
| 카드/아이템 종류 | §6 | 카드 `type`, 인벤토리 아이템 `kind`, 더미(pile) 이동 규칙 |
| 전투 진행/UI 개념 | §7 | 턴 페이즈, 적 의도(intent) 아이콘, 카드 더미 UI 라벨 |
| 장비 슬롯 | §8 | 무기/상의/하의/모듈/임플란트 장착 구조 |

---

## 🔴 1. 불일치 발견 (우선 검토 대상)

| 내부 키 | 현재 상태 (혼용 중) | 확정 명칭 |
|---|---|---|
| `ammo` | "탄약"(AmmoGauge, itemDisplay 일부) / "총알"(cards.js 설명, Card.js) / "탄"(cards.js 이전 표현) / "탄환"(LoadoutScreen "시작 탄환") | |
| `currency`(아이템 종류) | "환금템"(cards.js, itemDisplay.js) / "환급템"(RewardScreen.js, rewardTables.js 주석) | |
| `junk`(아이템 종류) | "잡템"(cards.js, itemDisplay.js) / "재료"(RewardScreen.js 카테고리 라벨, rewardTables.js 주석 일부) | |
| `block`(방어도) | 스펙 문서 정의는 "방어도" / UI(EnemyRow, PlayerStatusBar, Card) 표시는 "방어"로 축약 | |
| burden(과적) 관련 짐 표현 | "과적"(스펙 정식 용어) / "짐"(UI 축약, `LoadoutScreen.js` "짐(덱 카드)", `burdenIds` 등) | |

---

## 2. 자원 (Resources)

| 내부 키 | 현재 상태 | 확정 명칭 | 비고 |
|---|---|---|---|
| `energy` | 에너지 / 코스트(카드 소모값 자체는 "코스트") | | 카드 하나를 내는 데 쓰는 자원 이름은 "에너지", 카드에 적힌 소모량 표시는 "코스트"로 이원화되어 있음 |
| `ammo` | (위 표 참고) | | |
| `overload` | 과부화 | | 런 전체 유지, 0~100 |
| `stage` | 단계 | | 과부화 수치로 정해지는 0~3단계 |
| stage 별칭 (0~3단계) | 노멀 / 최적 / 과열 / 멜트다운 (`STAGE_LABELS`, `STAGE_ZONE_LABELS`) | | 스펙 §0 용어표엔 없던 표현. 게이지 UI 전용 별칭인지, 정식 명칭에 편입할지 결정 필요 |
| inventory 용량 초과 | 과적 (burden) | | 위 불일치 표 참고 |

---

## 3. 방어/전투 자원

| 내부 키 | 현재 상태 | 확정 명칭 | 비고 |
|---|---|---|---|
| `armor` (status) | 갑옷 | | |
| `block` | 방어도 (문서) / 방어 (UI) | | 위 불일치 표 참고 |
| `ambush` | 기습 | | |
| `reflect` | 반사 | | |

---

## 4. 상태이상 (Status Effects)

| 내부 키 | 분류 | 현재 상태 | 확정 명칭 | 비고 |
|---|---|---|---|---|
| `weak` | 디버프 | 약화 | | 공격자 기준 적용, 자신 공격력 ×0.75 |
| `vulnerable` | 디버프 | 취약 | | 피격자 기준 적용, 받는 피해 ×1.5 |
| `stun` | 디버프 (적 전용) | 스턴 | | |
| `armor` | 버프 | 갑옷 | | §3에도 중복 등재 (방어 자원이자 상태이상) |
| `reflect` | 버프 (플레이어 전용) | 반사 | | §3에도 중복 등재 |
| `atkBonus` | 버프 (적 전용) | 공격력 보너스 (스펙 문서) / "공격력+" (UI 라벨, statusEffects.js) | | 표기 축약 차이 — 통일 여부 결정 |
| `fragile` | 디버프 | 손상 | | **스펙 §4.2 표에 누락된 신규 상태이상.** 방어도 획득 0.75배. monsters.js에서 사용 중 |
| `entangled` | 디버프 | 뒤얽힘 | | **스펙 §4.2 표에 누락.** 공격 카드 코스트 증가 |
| `constrict` | 디버프 | 조이기 | | **스펙 §4.2 표에 누락.** 턴 종료 시 고정 피해 |

---

## 5. 모듈 파워 (Powers)

| 내부 키 | 현재 상태 | 확정 명칭 |
|---|---|---|
| `neuralBoost` | 신경 강화 | |
| `bodyBoost` | 신체 강화 | |
| `spatialAwareness` | 공간 지각 | |
| 역장 방어 | 역장 방어 (파워 목록엔 미포함, 일회성 갑옷 부여) | |
| 역장 방출 | 역장 방출 (소멸 카드, 광역 취약+약화) | |

---

## 6. 카드/아이템 종류

| 내부 키 | 현재 상태 | 확정 명칭 | 비고 |
|---|---|---|---|
| `junk` | 잡템 / 재료 | | 위 불일치 표 참고 |
| `currency` | 환금템 / 환급템 | | 위 불일치 표 참고 (오타성 불일치로 보임) |
| ammo item | 탄약 더미 | | |
| 무게(짐) 저주 카드 | 짐 카드 (스펙 §0) | | 저주 카드 형태로 인벤토리 초과분을 나타냄. "짐"이 §1의 "과적"과 다른 문맥에서도 쓰임 |
| 카드 type: `attack`/`skill`/`power`/`curse` | 공격 / 스킬 / 파워 / 저주 | | |
| `attackKind`: `melee`/`ranged` | 근접 / 원거리 | | |
| `exhausts` | 소멸 (카드가 이동하는 더미: 소멸 더미) | | |

---

## 7. 전투 진행 / UI 개념

| 내부 키 | 현재 상태 | 확정 명칭 | 비고 |
|---|---|---|---|
| draw pile / discard pile | "뽑을 카드" / "버린 카드" (PileCounts.js 라벨) / "카드 더미"(주석) | | 스펙 문서엔 이 개념 자체가 없음 (더미 명칭 신규 정리 필요) |
| 적 의도(intent) - 디버프 | 디버프 | | |
| 적 의도(intent) - 버프 | 버프 | | |
| 적 의도(intent) - 도주 | 도주 | | |
| 적 의도(intent) - 저주 카드 삽입 | 오염 (IntentIcon.js) | | **스펙 문서에 없는 표현.** 스펙 §2.7에서는 "저주 삽입"으로 서술됨 — "오염" vs "저주 삽입" 통일 필요 |
| 페이즈: `setup`/`player_turn`/`enemy_turn`/`victory`/`defeat` | 설정 / 플레이어 턴 / 적 턴 / 승리 / 패배 | | |

---

## 8. 장비 슬롯

| 내부 키 | 현재 상태 | 확정 명칭 |
|---|---|---|
| 무기 | 무기 (최대 2) | |
| 상의/하의 | 상의 / 하의 (각 1) | |
| 모듈 | 모듈 (최대 2) | |
| 임플란트 | 임플란트 (최대 3, 패시브) | |

---

## 사용 방법

1. 위 표의 "확정 명칭" 칸을 채워서 돌려주세요 (전부 안 채워도 되고, 🔴 1번 불일치 항목만 먼저 정해주셔도 됩니다).
2. 확정되면 그 명칭에 맞춰 데이터 텍스트(`src/data/*.js`)와 UI 라벨(`src/components/*.js`)을 일괄 수정하겠습니다.
3. `카드_전투_스펙.md` §0/§4.2 용어표도 이 문서 기준으로 갱신하겠습니다.
