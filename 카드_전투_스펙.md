# 카드 스펙 & 전투 규칙 문서

> 이 문서는 현재 코드(`src/engine`, `src/data`)의 **실제 구현 동작**을 기준으로 작성되었습니다.
> 기획서(`카드익스트랙션_프로토타입_기획서_1.md`)와 다른 부분은 "⚠️ 기획서와 차이" 로 표시했습니다.
> **이 문서를 수정하면 그 내용에 맞춰 코드를 고칩니다.** 즉 이 파일이 스펙의 기준(source of truth) 역할을 합니다.

---

## 0. 용어 정리

| 용어 | 의미 |
|---|---|
| 과부화 (overload) | 런(run) 전체에서 유지되는 게이지. 전투가 끝나도 리셋되지 않음. 0~100, 100 이상이면 즉사(패배). |
| 단계 (stage) | 과부화 수치로 정해지는 0~3단계. 카드/모듈 파워의 수치가 이 단계에 따라 스케일링됨. |
| 과적 (burden) | 인벤토리 용량을 초과해서 들고 있는 아이템. 짐 카드(잡템/환금템)로 변해 덱에 섞여 들어감. |
| 기습 (ambush) | 전투 시작 시 적이 선공하는 특수 시작 방식. |
| 갑옷 (armor) | 스택 형태의 방어 자원. 정해진 시점에 방어도(block)로 전환됨. |
| 방어도 (block) | 매 턴 초기화되는 피해 흡수 자원. |
| 반사 (reflect) | 다음 피격 시 공격자에게 되돌려주는 피해 스택. |

---

## 1. 카드 스펙

### 1.1 카드 정의 형태

카드는 두 가지 형태 중 하나로 정의됩니다.

**A. 일반 카드**
```
{
  id, name,
  type: 'attack' | 'skill' | 'power' | 'curse',
  attackKind: 'melee' | 'ranged' | null,
  cost,                // 에너지 코스트
  ammoCost?,           // 탄약 코스트 (있으면 탄약도 소모)
  exhausts: bool,       // true면 사용 후 소멸 더미로
  scalesWithStage: bool, // damage/block 값이 과부화 단계 배율을 받는지
  overloadGain: number, // 카드 사용 시 얻는 과부화(고정값)
  effects: [...],
  description,
  unplayable?: bool,     // 기본적으로 손패에서 낼 수 없음 (잡템/환금템 등)
  requiresWeapon?: string // 특정 무기 장착 시에만 덱에 편입
}
```

**B. 모듈 카드 (단계별 표, `stageTable`)**
```
{
  id, name, type, attackKind, exhausts, overloadGain,
  powerKind: 'variable' | 'fixed' | null,
  stageTable: [        // 0~3단계 각각의 값
    { cost, effects } | { cost, armorPerTurn },
    ...
  ],
  requiresWeapon?, description
}
```
카드를 낼 때 `stageTable[min(현재단계, 3)]` 이 실제 사용됨.

### 1.2 필드 설명

- **type**: `attack`(공격) / `skill`(스킬) / `power`(파워) / `curse`(저주). `power` 타입은 사용 후 버림/소멸 더미로 가지 않음(카드로서 소비되지 않음, 파워 활성화 개념).
- **attackKind**: `melee`(근접) / `ranged`(원거리) / `null`. 모듈 파워 보너스 적용 대상 판단, 그리고 `ignoresBlock`(방어 무시) 적용 여부(원거리 공격에만 허용)에 사용.
- **cost**: 에너지 코스트. 0도 가능(예: 조준 카드).
- **ammoCost**: 탄약 소모량. 있으면 카드 사용 가능 여부 판정 시 탄약 보유량도 확인.
- **exhausts**: 소멸 카드 여부. true면 사용 후 소멸 더미로 이동(버림 더미 아님).
- **scalesWithStage**: damage/block 수치가 과부화 단계 배율(아래 1.4 참고)의 영향을 받는지.
- **overloadGain**: 카드를 "사용"하는 순간 무조건 얻는 과부화(효과 처리 결과와 무관하게 적용됨). 임플란트로 배율 적용 가능.
- **powerKind**: 모듈 카드 전용. `variable`은 활성화 후 매 계산마다 현재 단계 기준으로 재계산되는 파워(예: 신경 강화). `fixed`는 사용 시점에 1회성으로 스냅샷되는 효과(예: 역장 방어의 갑옷 부여).
- **unplayable**: 기본적으로 낼 수 없는 카드. 잡템/환금템은 해당 아이템이 "과적" 상태일 때만 예외적으로 낼 수 있음.
- **requiresWeapon**: 지정된 무기가 함께 장착되어 있을 때만 덱 구성에 포함.

### 1.3 효과(effect) 종류

카드 하나는 `effects` 배열을 가지며, 각 원소는 아래 `kind` 중 하나입니다.

| kind | 필드 | 동작 |
|---|---|---|
| `damage` | `value, attackKind?, target?` | 대상에게 피해 (계산식은 2.3 참고) |
| `block` | `value, target?` | 방어도 획득 |
| `applyStatus` | `status, amount, target?` | 상태이상 스택 증감 |
| `applyStun` | `target: 'enemy', amount` | 지정한 적에게 스턴 부여 |
| `draw` | `count` | 카드 N장 드로우 |
| `discardRandomFromHand` | - | 손패에서 무작위 1장 버림 |
| `activatePower` | `power` | `player.powers[power]` 활성화 (지속형 모듈 파워) |
| `grantNextRangedBonus` | `amount, ignoresBlock` | 다음 원거리 공격 1회에 적용될 보너스를 예약 |
| `removeInventoryItem` | - | 연결된 인벤토리 아이템 영구 제거 (잡템/환금템 카드 전용) |

정의되지 않은 kind는 즉시 에러가 발생합니다(신규 효과 추가 시 반드시 처리 분기를 코드에 추가해야 함).

### 1.4 대상(target) 지정

`effect.target`을 생략하면 기본값이 적용됩니다:
- `block` → 자신(self)
- `applyStatus` → 자신(self)
- `damage` → 상대편(플레이어가 시전 시 적, 적이 시전 시 플레이어)
- 그 외 → 자신

명시 가능한 값: `self`, `player`, `all_enemies`, `enemy`/`machine_enemy`(플레이어가 UI에서 드래그로 지정한 대상).

### 1.5 특수 카드/키워드 예시

- **투시**: 다음 원거리 공격 1회가 방어도를 완전히 무시(`ignoresBlock`).
- **역장 방어**: `fixed` 파워. 사용 즉시 갑옷 스택을 1회성으로 부여(지속 파워로 등록되지 않음).
- **역장 방출**: 소멸 카드. 전체 적에게 취약+약화를 동시에 부여하는 광역기.
- **돌진 베기**: 카타나 장착 시에만 덱에 포함.
- **해킹**: 기계가 아닌 적에게도 스턴 적용 가능(테마와 무관하게 범용).
- **찌르기**: 자신에게 취약 1턴을 부여하는 대가로 고피해.
- **잡템/환금템**: 평소엔 낼 수 없음. 해당 아이템이 과적 상태일 때만 카드로 낼 수 있어 처리(제거) 가능.

---

## 2. 전투 진행 순서

### 2.1 페이즈

`setup` → `player_turn` → `enemy_turn` → (반복) → `victory` / `defeat`

### 2.2 전투 시작

- **일반 시작**: `startPlayerTurn` 을 바로 실행 (플레이어 선공).
- **기습(ambush)**: 적 턴을 먼저 1회 통째로 실행한 뒤(`enemy_turn`), 턴 수를 1 증가시키고 플레이어 턴 시작. 이 선제 적 턴 중 플레이어가 사망하면 플레이어 턴은 아예 시작되지 않음.
  - 기습 발생 조건: 전투 후 선택지(휴식/파밍)에 설정된 확률로 판정.

### 2.3 플레이어 턴 시작 시 처리 순서

1. 방어도(block) 0으로 초기화, 에너지를 최대치로 리셋(이전 잉여분 이월 없음).
2. 카드 5장(+임플란트 보너스만큼 추가) 드로우.
3. `phase = player_turn` 설정.
4. 특정 임플란트(산개 방출)가 있으면 전체 적에게 고정 광역 피해(약화/취약 배율 영향을 받지 않음 — 카드가 아닌 임플란트 고유 피해이므로).
5. 승패 판정(임플란트 피해로 마지막 적이 죽을 수 있으므로).

> 플레이어 턴 시작 시점에는 갑옷→방어도 전환이 일어나지 않습니다(아래 2.6 참고).

### 2.4 카드 사용 처리 순서

1. `player_turn` 페이즈인지, 낼 수 있는 카드인지 확인.
2. 손패에서 카드 제거.
3. 현재 과부화 단계에 맞는 코스트/효과 확정(모듈 카드의 경우).
4. **효과 처리보다 먼저** `overloadGain`만큼 과부화 획득(카드 결과와 무관하게 항상 적용).
5. 에너지(및 탄약) 소모.
6. `fixed` 파워(예: 역장 방어)는 일반 효과 처리를 건너뛰고 갑옷을 직접 부여.
7. 그 외 카드는 `effects` 순서대로 처리.
8. 원거리 보너스(투시 등)를 사용했다면 소모 처리.
9. 카드를 버림 더미 또는 소멸 더미로 이동(`power` 타입은 어느 더미에도 가지 않음).
10. 승패 판정.

### 2.5 코스트 보정 (모듈 파워)

과부화 단계에 따라 특정 파워가 활성화된 상태면 카드 코스트가 증가합니다.
- **3단계**: 신경 강화 활성 시 스킬 카드 코스트 +1, 신체 강화 활성 시 근접 카드 코스트 +1.
- **2·3단계**: 공간 지각 활성 시 원거리 카드 코스트 +1.

### 2.6 플레이어 턴 종료 처리 순서 (정확한 순서가 중요)

1. 승패 판정(이미 끝난 경우 조기 종료).
2. **갑옷 → 방어도 전환**: 보유한 갑옷 스택 전체 값만큼 방어도 획득, 그 후 갑옷 스택 1 감소(0이면 삭제).
3. 손패 전체 버림.
4. 임시 효과(사용하지 않은 원거리 보너스 등) 초기화.
5. **약화/취약 1턴 감소**(자기 자신의 것).
6. `phase = enemy_turn` 전환.

### 2.7 적 턴 처리 순서

적은 **배열 순서(생성 순서, = 표시 순서)** 대로 한 마리씩 순서대로 행동합니다(의도/우선순위 기반 아님). 각 적마다:

1. 방어도(block) 0으로 초기화.
2. "고정 갑옷 하한" 몬스터라면 현재 갑옷 스택이 하한보다 낮으면 하한까지 채움.
3. **갑옷 → 방어도 전환** (해당 적 자신의 턴이 "시작"할 때 — 플레이어와 반대 타이밍).
4. 행동 횟수 결정 (기본 1회, 특정 조건에서 2회 — 전투 시작 시점에 플레이어가 과적 아이템을 갖고 있었는지로 정해지며, 전투 중간에 재판정되지 않음).
5. 각 행동 슬롯마다:
   - 스턴 스택이 있으면: 스택 1 감소하고 이번 행동은 스킵(의도는 다시 뽑지 않고 유지됨).
   - 없으면: 예정된 행동(공격/효과/저주 삽입/재화 강탈/자폭/도주 등) 실행. 공격이 여러 타(hits)일 경우 타마다 개별적으로 피해 계산(약화/취약/방어 흡수도 타별로 적용).
   - 행동마다 승패 판정. 플레이어가 죽으면 그 즉시 중단(남은 적들은 행동하지 않음).
6. 이 적의 모든 행동이 끝난 뒤: **이 적의 약화/취약 1턴 감소**.
7. AI 진행 인덱스 갱신 및 다음 의도(표시용) 미리 뽑기.

### 2.8 전체 턴 넘김 순서

```
플레이어 턴 종료 처리 → (게임 안 끝났으면) 적 턴 전체 처리 → (게임 안 끝났으면) 턴 수 +1 → 플레이어 턴 시작
```

### 2.9 대상 지정

- 플레이어는 카드 사용 시 대상을 직접 지정(단일 대상 카드의 경우).
- `all_enemies` 효과는 살아있는(HP>0, 도주하지 않은) 모든 적에게 개별적으로 적용(취약 배율도 적마다 따로 판정).
- 적의 공격은 항상 플레이어만을 대상으로 함(적끼리는 서로 공격하지 않음).

### 2.10 자원

- **에너지**: 최대치 3, 매 플레이어 턴 시작 시 최대치로 리셋(이월 없음). 유일한 카드 사용 자원.
- **탄약**: 턴마다 리셋되지 않고 전투/전투 사이에도 유지되는 별도 자원. 인벤토리 칸을 차지하지 않음.

### 2.11 보스 페이즈 전환

적 범위 피해가 적용될 때마다 검사됨. HP가 임계 비율 이하로 떨어지면 1회 트리거되어 페이즈를 전환하고(AI 시퀀스 초기화, 페이즈2 시퀀스로 교체), 지정된 저주 카드를 버림 더미에 삽입.

### 2.12 승패 판정

- 플레이어 HP ≤ 0, 또는 과부화가 즉사 기준(100) 이상이면 패배.
- 살아있는 적이 하나도 없으면 승리.
- 카드 사용/적 행동/임플란트 광역 피해 직후마다 매번 판정됨.

---

## 3. 전투 규칙

### 3.1 피해 계산 순서

```
값 = 단계 배율 적용(base, 현재 단계, scalesWithStage 여부) + 고정 보너스(모듈 파워 등)
공격자가 약화 상태면: 값 = floor(값 × 0.75)
(← 여기까지 공통 계산, 이후는 대상별로 개별 처리)
대상이 취약 상태면: 값 = floor(값 × 1.5)
최종 값 = max(0, 값)
```
- 단계 배율이 먼저 적용되고, 고정 보너스는 그 이후에 더해짐(보너스 자체가 이미 단계별로 산정된 값).
- **약화**는 공격자 기준으로 피해 계산 시점에 한 번만 적용됨.
- **취약**은 대상 기준으로 별도로, 광역기라면 적마다 따로 적용됨.

### 3.2 방어도 계산

```
값 = 단계 배율 적용(base, 현재 단계, scalesWithStage 여부) + 고정 보너스
최종 값 = max(0, 값)
```
방어도를 약화시키는 상태이상(예: 허약)은 존재하지 않음.

### 3.3 피해 흡수

```
방어 무시(ignoresBlock)면: HP -= 값 (방어도 그대로)
아니면: 흡수량 = min(방어도, 값); 초과량 = 값 - 흡수량
        방어도 -= 흡수량; HP -= 초과량
```
방어 무시는 "투시" 보너스 + 원거리 공격 조합에서만 발생.

### 3.4 갑옷 → 방어도 전환

```
갑옷 스택 ≤ 0 이면 아무 일도 없음
방어도 += 갑옷 스택 (전환 직전의 값, 감소 전 값 그대로)
갑옷 스택 -= 1 (0이면 삭제)
```
전환 시점: **플레이어는 자신의 턴이 끝날 때**, **적은 자신의 턴이 시작할 때**.

### 3.5 과부화 게이지

- 최대 100, 런 전체에 걸쳐 유지(전투 종료로 리셋되지 않음).
- 단계: 0~24 → 0단계, 25~49 → 1단계, 50~74 → 2단계, 75~99 → 3단계, 100 이상 → 즉시 패배.
- 과부화 획득: `round(수치 × 배율)`만큼 더함. 배율은 특정 임플란트로 0.5(절반) 가능.
- 과부화 감소: 하한선(장착한 임플란트들의 하한 합) 아래로는 내려가지 않음. 감소 수단: 소모품(안정제 -20), 전투 후 선택지(휴식 -30).
- 단계 배율(카드 수치 계산용): 1·2단계는 `round(base × 1.25)`, 0·3단계는 `base` 그대로(3단계에서 기본값으로 되돌아가는 것은 의도된 설계).

### 3.6 장비 / 덱 구성

- 무기/상의/하의/모듈은 각각 카드 목록(`cardList`)을 가지며, 장착 시 해당 카드들이 통째로 런 덱에 추가됨.
- 슬롯 제한: 무기 최대 2, 모듈 최대 2, 임플란트 최대 3, 상의/하의 각 1.
- 임플란트는 카드가 없는 패시브. 과부화 하한, 최대 HP 보너스, 전투 시작 디버프, 인벤토리 보너스, 추가 드로우, 과부화 획득 배율, 턴 시작 광역 피해 중 하나를 부여.
- 덱은 **매 전투 시작마다** 새로 구성됨(장비 기반 카드 + 그 시점에 과적 상태인 짐 카드).
- 모듈 파워(신경 강화/신체 강화/공간 지각)는 한 번 활성화되면 전투 끝까지 유지되며, 매 계산 시점의 현재 단계를 기준으로 실시간 재계산됨(스냅샷 아님).

### 3.7 인벤토리 / 과적

- 인벤토리는 고정 용량(+임플란트 보너스)을 가짐.
- 용량을 초과해 획득한 순서대로의 아이템이 "과적" 상태가 됨(가치 기준이 아니라 획득 순서 기준).
- 과적 아이템은 잡템/환금템 카드로서 덱에 편입되어 전투 중 처리(제거)할 수 있음. 처리하면 다음으로 오래된 초과 아이템이 자동으로 정상 상태로 승격.
- 무게(짐) 저주 카드(소멸형)는 별도의 "다음 전투로 이월" 플래그 없이, 매 전투마다 장비 목록에서 덱을 다시 만드는 과정에서 사실상 다시 등장함.

### 3.8 기습

전투 후 선택(휴식/파밍)에 설정된 확률로 판정되어, 성공 시 해당 전투가 적 선공으로 시작됨.

---

## 4. 상태이상 용어와 적용 방식

### 4.1 공통 규칙

상태이상 스택은 `applyStatus(대상, 종류, 증감량)`으로 가감됩니다. 값이 0 이하가 되면 해당 상태이상은 삭제됩니다.

### 4.2 전체 목록

| 내부 키 | 한글 명칭 | 분류 | 부여 방식 | 스택 의미 | 감소/소멸 시점 | 상호작용 |
|---|---|---|---|---|---|---|
| `weak` | 약화 | 디버프 | 카드/적 행동으로 자신·적·플레이어 등에 부여 | 스택 수 = 지속 턴 수 | 보유자 **자신의 턴이 끝날 때** 1 감소 | 피해 계산 시 **공격자** 기준으로 적용, 자신의 공격력을 ×0.75 |
| `vulnerable` | 취약 | 디버프 | 카드/적 행동으로 부여 (자해 카드도 존재) | 스택 수 = 지속 턴 수 | 보유자 자신의 턴이 끝날 때 1 감소 | 피해 계산 시 **피격자** 기준으로 적용, 받는 피해를 ×1.5 (대상별 개별 판정) |
| `armor` | 갑옷 | 버프 | 특정 카드(역장 방어), 특정 몬스터의 고정 하한 | 스택 수 = 전환 시 얻을 방어도 양 | 3.4절의 전환 규칙에 따라 소모(플레이어: 턴 끝, 적: 턴 시작) | 전환될 때 방어도를 즉시 부여, 그 자체로는 피해를 줄이지 않음 |
| `stun` | 스턴 | 디버프 (현재 구현상 적 전용) | `applyStun` 효과(예: 해킹 카드)로 특정 적에게 부여 | 스택 수 = 스킵할 행동 횟수 | 스킵되는 행동마다 1 감소 (턴 종료 시 자동 감소 아님) | 스킵된 행동은 의도를 다시 뽑지 않고 다음 행동 슬롯에 그대로 이어짐 |
| `reflect` | 반사 | 버프 | 특정 카드(튕겨내기)로 자신에게 부여 | 스택 수 = 반사할 고정 피해량 | 다음 피격 시 전량 소모(턴 기반 아님) | 공격해온 적에게 그만큼 피해를 되돌림(그 적의 방어도는 정상적으로 적용됨). 현재 구현상 플레이어만 보유 가능 |
| `atkBonus` | 공격력 보너스 | 버프 (현재 구현상 적 전용) | 특정 적의 포효/이 갈기류 행동으로 자신에게 부여 | 스택 수 = 고정 추가 피해량 | 별도 감소 없음(전투 끝까지 유지, 중첩 가능) | 피해 계산의 고정 보너스로 취급되어 단계 배율/약화/취약과 동일한 파이프라인을 거침 |

### 4.3 상태이상과 별개인 "모듈 파워"

다음은 `player.powers`에 저장되는 별도 개념으로, 위 상태이상 표와는 다릅니다. 카드로 활성화하면 전투가 끝날 때까지 유지되며, 매 계산 시점마다 **현재 과부화 단계**를 기준으로 실시간 재계산됩니다(스냅샷 아님).

| 파워 | 한글 명칭 | 효과 |
|---|---|---|
| `neuralBoost` | 신경 강화 | 방어도 획득에 단계별 보너스 |
| `bodyBoost` | 신체 강화 | 근접 공격 피해에 단계별 보너스 |
| `spatialAwareness` | 공간 지각 | 원거리 공격 피해에 단계별 보너스 |

역장 방어는 위 파워 목록에 포함되지 않습니다 — 사용 시 그냥 갑옷 상태이상을 1회성으로 부여할 뿐, 지속 파워로 등록되지 않습니다.

### 4.4 틱/소멸 시점 요약표

| 시점 | 발생하는 일 |
|---|---|
| 플레이어 턴 시작 | 방어도 0, 에너지 최대치로 리셋, 카드 드로우, (임플란트) 턴 시작 광역 피해(약화/취약 영향 없음) |
| 플레이어 턴 종료 | ① 갑옷→방어도 전환 및 갑옷 -1 ② 손패 버림 ③ 임시 효과 초기화 ④ 약화/취약 -1 |
| 적 개별 턴 시작 (적 턴 처리 루프 내부) | ① 방어도 0 ② 고정 갑옷 하한 보충 ③ 갑옷→방어도 전환 및 갑옷 -1 ④ 행동마다 스턴 체크(스킵+감소) ⑤ 행동 실행 ⑥ (모든 행동 후) 약화/취약 -1 ⑦ AI 진행 |
| 피격 시(플레이어) | 반사 스택이 있고 공격자가 특정되면, 반사 스택 전량 소모해 공격자에게 피해(공격자 방어도 적용됨) |
| 카드/행동 처리 | 카드 사용 시 효과 처리 전에 과부화 획득 확정; 적 범위 피해 적용마다 보스 페이즈 전환 체크 |

---

## 5. 알려진 기획서 ↔ 코드 불일치 (⚠️ 확인 필요)

이 항목들은 기획서와 실제 코드 동작이 다른 부분입니다. 이 문서를 수정할 때 어느 쪽이 맞는지 확정해 주세요.

1. **인벤토리 기본 용량**: 기획서는 30칸, 코드는 20칸(`BASE_INVENTORY_CAPACITY`).
2. **소모품 코스트**: 기획서상 코스트를 지불하고 사용하는 것으로 되어 있으나, 코드에서는 코스트를 전혀 차감하지 않음(무료 사용).
3. **무게(짐) 저주의 "이월" 플래그**: 기획서는 명시적 플래그를 언급하나, 코드는 매 전투 덱 재구성 시 암묵적으로 재등장하는 방식으로만 구현됨.
4. **과부화 구간/사망 규칙**: `수정사항.md`에 적힌 "0~30/30~60/60~90/90~100%, 100%여도 사망 안 함, 매턴 10 감소" 등은 **미구현 상태**입니다. 현재 코드는 0-24/25-49/50-74/75-99/100+(즉사) 구간을 사용합니다. `수정사항.md`의 내용을 반영할지 이 문서에서 먼저 정해주세요.
5. **§7.1의 "근접 공격력+/원거리 공격+/방어력+" 상태이상**: 실제로는 상태이상이 아니라 모듈 파워(4.3절)로 구현되어 있습니다.
