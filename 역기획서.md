# 역기획서 — 카드 익스트랙션 (as-built, 2026-08-23)

> **이 문서의 성격**: 기존 `기획서.md`/`카드_전투_스펙.md`/`docs/extraction-map-implementation-spec.md`는 모두 "의도(기획) → 코드"를 지향하는 순방향 문서다. 이 문서는 반대로 **현재 `src/` 코드(작업 트리 미커밋 변경분 포함)를 직접 읽어 역산**한 "지금 실제로 굴러가는 게임이 무엇인가"의 스냅샷이다. 문서와 코드가 어긋난 지점은 8장에 별도로 정리했다.
>
> 작성 기준: 2026-08-23 기준 `git status`의 작업 트리(다수 파일이 아직 커밋되지 않은 상태 — 시설맵 리팩터와 전투 엔진 변경이 함께 걸쳐 있음). 수치는 전부 코드에서 직접 읽은 값이며, 기획 문서의 수치와 다를 경우 코드 쪽을 채택했다.

---

## 1. 장르 요약 (실제 구현 기준)

카드 배틀(슬레이 더 스파이어류) + 절차적 익스트랙션 맵(타르코프류)을 한 세션 안에서 잇는 로그라이트. 순수 함수형 엔진(Preact 없이 `node --test`로 헤드리스 실행 가능) 위에 Preact/HTM UI가 얹혀 있고, `state/dispatch.js`가 유일하게 signal과 `gameReducer`를 연결한다.

한 판(런)의 실제 흐름:

```
LoadoutScreen(출격 준비) → MapScreen(160노드 시설 탐사, 시간/위협/소음 시뮬레이션)
   ↕ (위협과 조우하면 강제 전환)
CombatScreen(카드 전투) → RewardScreen(파밍/전투 승리 보상)
   ↘ 사망 시 GameOverScreen / 탈출 성공 시 ExtractionCompleteScreen
```

화면 목록은 `src/components/App.js`의 `SCREENS` 테이블이 그대로 유한 상태 기계다: `loadout | map | combat | reward | gameOver | extractionComplete`.

---

## 2. 시설맵 — 절차적 침투 시뮬레이션

### 2.1 그래프 생성 (`facilityGraph.js`, `data/facilityLayout.js`)

- **8구역 × 20노드 = 160노드** (구 버전은 4구역×12=48노드였고, 최근 리팩터로 규모가 3배 이상 확장됨). 구역: 입구·관리동 / 실험동 / 격납고·물류창고 / 보안·격리동 / 동력·정비동 / 폐기물 처리장 / 통신·관제탑 / 거주동(`SECTOR_IDS`).
- 배치는 완전히 기하학적이다: 8구역을 하나의 큰 링 위에 균등 배치(`SECTOR_RING_RADIUS=900`)하고, 각 구역 안에서 노드 20개를 원판 안에 거부 샘플링으로 흩뿌린다(`SECTOR_NODE_RADIUS=280`, 최소 간격 55). 이전 세대(단일 사이클 기반 48노드)와 달리 순수 좌표 기반이라, 일반 엣지는 "전역에서 가장 가까운 노드 쌍부터" 연결하는 차수 제한(2→4) Kruskal식 알고리즘 + Tarjan 브릿지 복구 3단계 패스로 만들어진다.
- 구역 내부는 촘촘히 연결되지만, **구역과 구역 사이는 일반 엣지로는 거의 연결되지 않는다** — 오직 "특수 엣지"(구역 내부용 4~6개, 인접 구역 간 2~3개, 비인접 구역을 잇는 초희귀 원거리 지름길 2~3개 전체)로만 넘나든다. 즉 지도 위상 자체가 "구역 = 관문으로만 이어진 섬"이다.
- 특수 엣지는 `oneWay`(환풍구류, 일방통행) / `blocked`(잠긴 문·바리케이드) / `electronic`(카메라·검문) / `highGround`(Mobility 3 이상 필요) 4종 태그를 확률 가중치로 받고, 30% 확률로 2번째 태그가 추가로 붙는다(예: 전자 보안이 걸린 잠금문).
- 엣지 시간 비용은 더 이상 균일 상수가 아니라 **두 노드의 2D 거리에 비례**(`EDGE_TIME_PER_LENGTH_UNIT=2`, 40~260 클램프)한다. 탈출구 가중거리 계산도 홉수가 아니라 Dijkstra다.
- 그래프 생성은 실패할 수 있다(탈출구 거리 조건/2-엣지-분리경로 조건 불충족) — 최대 64회 재시도, 그래도 실패하면 시드 0부터 최대 256개를 훑어 캐시해 둔 폴백 토폴로지를 쓴다.

### 2.2 탈출구 3종

| 탈출구 | Mobility 0 가중거리 | 규칙 |
|---|---|---|
| A | 3200~5000 | 8600시점에 비활성화 |
| 열쇠(key) | 4600~6700 | 시간 제한 없음, 열쇠 소지 시 도착 즉시 추출 |
| B | 6000~7600 | 13400시점에 비활성화(→붕괴 유예 시작) |

세 탈출구는 서로 다른 구역에 배치되고, 시작점부터 항상 2-엣지-분리경로가 보장된다. A/B는 독립적으로 개방 요청 가능(요청 50 시간, 이후 유효 Hacking(-2~4)에 따라 300~100 시간 대기, 열리면 100 시간 동안만 즉시 추출 가능하고 닫히면 재요청 가능). 런 붕괴(게임오버) 시점은 **14000**(구 버전 4000 대비 3.45배 확장 — 160노드 스케일에 맞춘 재조정).

### 2.3 시간·틱 엔진 (`runEngine.js`)

- `advanceTime`이 모든 시간 경과의 단일 진입점. 10 시간 포인트마다 "월드 틱"이 발생해 위협 AI가 목표를 재평가·이동한다. 그 외에도 매 시각(경계 지점)마다 붕괴 판정, 출구 상태 전이, 소음/가짜 목표 만료가 처리된다.
- **행동 목표 우선순위**(위협 AI): 직접 목격/추격(pursuit) > 활성 탈출 신호(exitSignal) > 가장 크게 들리는 소음/가짜 목표(홉수로 감쇠되는 score) > 순찰(patrol). 소음은 세기(1~3)에 따라 도달 홉 범위(1/2/3)가 다르다.
- 위협 상태는 `patrol → investigate/alert → pursuit → exit_guard`의 모드 기계이며, 추격은 강도(pursuitStrength 0~3)가 매 접근마다 1씩 감소하다가 0에서 순찰로 복귀한다. 구역 경계도(0~3)가 오르면 조사/경계 이동 간격이 빨라진다(80→70→60).
- 카메라: 노드의 22%에 배치, Stealth 3 미만으로 지나가면 3홉 이내 모든 위협이 즉시 추격 모드로 전환. Hacking으로 해킹(300 지속) 또는 Force로 파괴(영구) 가능. 접속 인터페이스(16% 배치)를 해킹하면 같은 구역 전체 범위 정찰이 열린다.
- 발전기: `power`/`labs` 구역에 하나씩. 비활성화하면 전투 시작 시 적에게 주던 5 갑옷 보너스(`GENERATOR_COMBAT_START_ARMOR`)가 사라지는 것으로 추정(관련 상수만 확인, 실제 소비처는 combatReducer 쪽).

### 2.4 Capability 6종 — 이동/은신/해킹/파괴/속임/지각

`capabilityEngine.js`가 로드아웃(무기/상하의/모듈/임플란트)에 붙은 `MAP_EQUIPMENT_CAPABILITIES` 계약을 합산해 -2~4 범위로 클램프한다. 요구치 판정은 `max(0, value)`.

- **Mobility**: 이동 시간에 배율(-2:1.4배 느림 ~ +4:0.6배 빠름)로 직결.
- **Stealth**: 이동 소음(0~3, Stealth 4에서 완전 무음) + 흔적 등급(2 이하면 흔적 남김, -2 이하는 강한 흔적) 결정.
- **Hacking**: 카메라/발전기/접속 인터페이스 원격 제어 범위와 탈출구 개방 대기 단축.
- **Force/Hacking 공용**: `blocked`/`electronic` 특수 엣지 개방(tier 1만 구현, 더 높은 티어는 스코프 밖).
- 공통 접근 모드(안전/보통/강행)가 모든 현장 행동에 걸린다: 안전 = +40시간/-1소음, 강행 = -40시간(최소 20)/+1소음(최대3).

### 2.5 현장 행동 목록

이동, 기본 정찰(80시간, 항상 성공, 현재+인접 노드 위협 유무만 확정), 파밍(현장 기회 소모 — 노드당 0~2개, 각각 1~3회 사용 가능, 1% 확률로 탈출 열쇠), 특수 엣지 개방, 카메라 해킹/파괴, 접속 인터페이스 해킹, 발전기 무력화, 능동 현장 장비(장착 장비가 제공하는 `snapshot_scan`/`remote_intrusion`/`temporary_barrier` 세 종류 — 인스턴스별 쿨다운). 모든 시간 소모 행동 뒤에는 공통으로 "위협이 내 노드에 도착했는가"를 검사해 강제 전투로 전환한다(`triggerCombatIfNeeded`).

### 2.6 안개(관측) 모델

노드/엣지의 존재 자체는 가리지 않는다(전체 지도가 항상 보임) — 대신 "이 노드에 위협이 있는가"만 마지막으로 확인한 시각과 함께 스냅샷으로 남는다(`observations`). 시야를 벗어나도 지워지지 않고 "최신 정보 vs 오래된 정보"로 구분해 보여준다는 설계.

---

## 3. 전투 시스템

### 3.1 턴 구조

에너지 3(기본), 손패 5장, 매 턴 드로우. `setup → player_turn ⇄ enemy_turn → victory/defeat`. 기습(ambush)이면 적이 먼저 1턴 행동한 뒤 플레이어 턴으로 진입.

### 3.2 과부화(Overload) — 런 전체 유지 자원, 3단계

`overloadEngine.js`가 정의하는 규칙:

| 단계 | 구간 | 배율 |
|---|---|---|
| 0 (노멀) | 0~29 | ×1.0 |
| 1 (강화·예열) | 30~69 | ×1.25 (반올림) |
| 2 (강화+페널티·과열) | 70+ | ×1.25 + 모든 카드 코스트 +1 |

- **100 초과는 즉사가 아니다.** `combatEngine.js`의 `computeOverloadCurseCount`가 초과분을 `ceil(초과/10)`장의 사용불가 저주 카드로 환산해 그 전투에만 버림 더미에 삽입한다(전투 시작 시 최초 삽입 + `playCard` 시마다 재계산). 값 자체는 클램프되지 않고 계속 누적된다.
- 맵에서는 붕괴/멜트다운 상태가 완전히 사라졌다 — `MapScreen.js`가 100을 넘길 행동 자체를 사전에 막는다. `isLethalOverload`/`OVERLOAD_MAX`는 죽은 코드로 코드 주석에 명시돼 있다.
- `overloadFloor`(임플란트 비용 합)는 절대 이보다 낮아지지 않는 바닥값, `overloadGainMultiplier`는 임플란트로 조정 가능한 획득 배율. 맵-전투 간에 `withFacilityRunState`가 매 시설 액션마다 `playerState.overload`와 동기화한다(런 전체 공유 자원).
- 카드 수치 스케일링(`applyStageScale`): 단계 1·2 공통으로 데미지/방어도 +25%(반올림) — 기획 문서가 말하는 "5단계 세분화(0-24/25-49/…)"는 코드에 없다. **3단계로 이미 개편 완료.**

### 3.3 탄약 — 재고/장전 이미 분리됨

`createCombatState`가 전투 시작 시 인벤토리 재고(reserve)에서 무기들의 `maxLoadBonus` 합만큼 자동으로 `loaded`를 채우고 나머지는 reserve로 남긴다. 카드의 `reload` 효과(자동권총/리볼버/샷건/로켓런처/저격총 각 무기가 보유)로 전투 중 reserve → loaded 이동. 재고 고갈 시 총기 카드는 재장전으로도 회복 불가 — 하드 페널티. **기획서.md §3.2가 "미구현"이라 적어 둔 항목이 실제로는 이미 구현돼 있다.**

### 3.4 장비 내구도 — 이미 구현됨

`equipmentEngine.js`: 최대 내구도 10. 드랍 장비는 3~9 랜덤(시작 장비는 항상 10). 내구도 4 미만인 장착 장비마다 `(4-내구도)`장의 범용 "손상된 장비" 저주 카드가 전투 덱에만 삽입된다. 전투 중 장비 소속 카드를 낼 때마다 1% 확률로 그 인스턴스에 손상 1회가 누적되고(`durabilityDecayInstanceIds`), 전투 종료 후 실제로 적용돼 0이 되면 슬롯에서 자동 해제(파괴, 수리 시스템은 없음). **기획서.md §3.3이 "미구현"이라 적어 둔 항목도 이미 구현돼 있다.** → 8장 참고.

### 3.5 상태이상 (13종, `data/statusEffects.js`)

약화(0.75배 가해 피해)/취약(1.5배 피해)/갑옷(턴 경계에 방어도 전환)/스턴(행동 스킵)/반사/공격력+/손상(방어도 0.75배)/뒤얽힘(공격 코스트 가산)/조이기(시전자 생존 시 매턴 고정 1피해)/인공물(디버프 스택당 1회 무효화)/힘(가하는 피해 고정 가산, 감소 없음)/민첩(획득 방어도 고정 가산, 감소 없음)/중독(턴 시작 고정 피해, 방어도 무시).

### 3.6 카드 특수 태그

소진(exhausts) / 단계 스케일링(scalesWithStage) / 선천성(innate, 첫 턴 확정 드로우) / 보존(retain, 턴 종료 버려지지 않음) / 휘발성(volatile, 손패에 남으면 소진) / 교활(sly, 버려지면 무료 자동 발동) / 다단히트(hits) / 조건부 스케일링(scalesBy: 손패수/소진더미수/버림더미수/힘 스택/내 방어도/적 취약 스택/적 중독 스택/장전량) — 스탠다드 슬레이 더 스파이어 보캐뷸러리를 카드 익스트랙션 맥락(장전량 등)으로 확장한 형태.

### 3.7 몬스터/AI

21종 몬스터(일반 14 / 엘리트 3 / 보스 4 / 소환 전용 미니언 1). 시퀀스는 카드 이펙트와 동일한 인터프리터를 공유하며, `{random: [...]}` 분기는 실행 직전이 아니라 인텐트 표시 시점에 1회 확정되어(캐시) UI 인텐트와 실제 결과가 항상 일치하도록 보장. 보스는 HP 비율 기준 2페이즈 전환(시퀀스 교체 + 저주 카드 삽입) 지원.

---

## 4. 로드아웃 / 장비 / 인벤토리

- 덱빌딩 = 로드아웃 구성. 무기(최대 2슬롯, 빈 슬롯당 맨손공격 3장 보충) 9종, 상의 2종, 하의 2종, 모듈 7종(최대 2슬롯), 임플란트(플로어 과부화/최대체력/인벤토리 용량/과부화 획득 배율/추가 드로우/턴시작 광역피해 등 효과 제공, 개수 미확인·implants.js grep 실패로 정확한 종수는 별도 확인 필요), 소모품 슬롯 3칸.
- 카드 총 58종(`cards.js`).
- 인벤토리 30슬롯(+임플란트 보너스), 무게 개념 없음(1아이템=1슬롯). **용량 초과분(과적)은 짐 카드로 변해 덱에 섞이되, "플레이 가능"으로 전환**(코스트 1로 영구 버리기) — 즉사/페널티가 아니라 손패 오염과 기회비용으로 압박하는 마찰 자원.
- 워하우스(홈베이스 보관함)는 무제한 용량, 과적 규칙 미적용.

---

## 5. 보상/파밍

파밍 보상은 정상(normal) 65% / 정예(elite) 35%로 티어를 무작위 배정한 뒤 `rollRewardSlots`로 슬롯을 굴린다. 전투 승리 보상도 별도 경로(`rewardEngine.js`/`rewardTables.js`/`dropTables.js`, tier normal/elite/boss별 로직)로 존재하나 이번 조사에서 세부 수치까지는 확인하지 않았다.

---

## 6. 코드 아키텍처 스냅샷

- **엔진(순수 함수, `src/engine/*`)**: `runEngine`(맵 시간/위협/소음/탈출), `facilityGraph`(맵 생성), `facilityReducer`(맵 커맨드), `combatEngine`(전투 상태 기계), `cardEngine`(카드 더미 조작), `overloadEngine`, `capabilityEngine`, `equipmentEngine`(로드아웃→덱/내구도/스탯), `inventoryEngine`/`inventoryReducer`, `monsterAI`, `statusEngine`, `rewardEngine`/`rewardReducer`, `combatReducer`(전투 진입/이탈), `combatMapIntegration`(맵↔전투 다리), `loadoutReducer`, `historyEngine`(undo/redo), `rng`(mulberry32 기반 결정론 RNG — `Math.random` 절대 미사용).
- **의존 방향 강제**: `inventoryReducer ← rewardReducer ← combatReducer ← facilityReducer`, `loadoutReducer`는 `inventoryReducer`+`runEngine`에만 의존. 순환 없음(코드 주석에 명시된 설계 규율).
- `gameReducer.js`는 순수 커맨드 디스패치 테이블일 뿐 — 각 커맨드 패밀리의 로직은 각자의 리듀서 모듈에 있다.
- **결정론**: 시드 하나로 그래프/전투/드랍이 전부 재현 가능하다는 전제가 전체 설계를 관통한다(`rngState`를 모든 함수가 명시적으로 스레딩, 모듈 전역 카운터 금지 — 주석에서 반복적으로 강조됨).
- UI는 Preact + HTM(`lib.js`), 상태는 `state/runState.js`의 signal + `state/dispatch.js`.

---

## 7. 데이터 규모 (실측)

| 항목 | 개수 |
|---|---|
| 카드 정의 | 58 |
| 몬스터 정의 | 21 (일반 14 / 엘리트 3 / 보스 4 / 미니언 1) |
| 무기 | 9종 (카타나/자동소총/단검/권총/자동권총/리볼버/샷건/로켓런처/저격총) |
| 상의/하의 | 2종씩 |
| 모듈 | 7종 |
| 시설맵 | 160노드 × 8구역(구역당 20노드) |
| 소모품/임플란트 | 파일 존재 확인만, 개수 미집계 |

---

## 8. 문서-코드 괴리 (중요)

기존 `기획서.md`(§5 "구현 상태")는 다음을 **미구현**이라고 명시하고 있으나, 실제 `src/engine`을 읽어보면 **이미 구현·동작 중**이다:

1. **과부화 3구간 재설계** — `기획서.md`는 "코드는 여전히 0-24/25-49/50-74/75-99/100+ 즉사 구간을 따른다"고 적었지만, `overloadEngine.getStage`는 이미 0/1/2 3단계이고 `combatEngine`도 100 초과 즉사 로직이 없다(저주 카드 전환으로 대체됨).
2. **탄약 재고/장전 분리** — `PlayerCombatState.loaded/reserve/maxLoad`, `reload` 카드 이펙트, 전투 시작 자동 장전까지 전부 존재.
3. **장비 내구도 + 손상 저주 카드** — `equipmentEngine.js`의 `MAX_DURABILITY`, `applyDurabilityDecay`, `computeDamagedCurseEntries`가 완전한 파이프라인으로 존재.

즉 `기획서.md`/`카드_전투_스펙.md`는 이 조사 시점 기준 **최소 한 세대 뒤처진 스냅샷**이며, 실제 소스가 더 앞서 있다. 반대로 시설맵 쪽은 `docs/extraction-map-implementation-spec.md`가 "48노드"를 현재 기준으로 서술하고 있지만 코드는 이미 **160노드/8구역**으로 전면 개편된 상태(문서의 "48노드"·"4구역"·"RUN_COLLAPSE_TIME=4000" 서술은 전부 낡은 값 — 실제 상수는 각각 160/8/14000). 두 문서 모두 이번 작업 트리의 미커밋 변경분을 온전히 반영하지 못한 상태로 보인다.

미확정/미구현으로 코드에서도 확인되는 것:
- 발전기 무력화가 전투 시작 시 적 갑옷 보너스에 미치는 정확한 연결부(상수만 확인, 실제 소비 지점 미추적).
- 소모품/임플란트의 구체 종수와 효과 목록.
- 정화/역이용 카드(기획서가 언급한 과부화 완화 수단) — 카드 데이터에서 존재 여부 미확인.
- 장비 수리 수단(내구도는 구현됐지만 회복 경로는 안 보임 — 파괴만 있고 복구가 없어 보임).

---

## 9. 한 줄 요약

절차적 160노드 시설 침투(시간·소음·위협 AI가 실시간으로 상호작용)와 슬레이 더 스파이어식 카드 전투를 하나의 시드-결정론적 상태 기계로 묶은 익스트랙션 로그라이트. 과부화(카드 강화 대체)·탄약 재고/장전·인벤토리 과적·장비 내구도라는 4개 압박 축이 전부 실제 코드에 존재하며, 그 중 과부화·탄약·내구도 3개는 최상위 기획 문서가 아직 "미구현"이라고 잘못 기록하고 있는 상태다.
